-- (2)
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ
BEGIN TRAN
    SELECT * FROM STUDENTS WHERE SID='00000000'
    WAITFOR DELAY '00:00:10'
    UPDATE STUDENTS SET grade=4000 WHERE SID='00000000'
COMMIT TRAN
    SELECT * FROM STUDENTS WHERE SID='00000000'
